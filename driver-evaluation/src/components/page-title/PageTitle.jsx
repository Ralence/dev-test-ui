import React from "react";
import styles from "./pageTitle.module.css";

const PageTitle = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Driver evaluation</h1>
    </div>
  );
};

export default PageTitle;
